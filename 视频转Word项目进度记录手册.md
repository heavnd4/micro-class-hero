# 视频转Word自动化系统 - 项目进度记录手册

本手册专门记录“视频转Word自动化系统”项目的开发迭代、Bug 修复及功能演进。

---

## 📅 2026-04-24 (阶段 3：深度智能化与 GUI 体验大版本)

### 1. 智能 OCR 截图优化 (核心升级)
*   **变动检测**：引入 FFmpeg 场景检测 (`gt(scene,0.005)`)。
*   **语义去重**：集成 `PaddleOCR`。通过对相邻截图文本的交集计算相似度（阈值 80%），自动识别 PPT 翻页。
*   **极致稳定性（终极封锁方案）**：
    *   **环境兼容**：解决了 Paddle 3.x 在 Windows 下的 oneDNN/PIR 兼容性崩溃。
    *   **入口拦截**：在 GUI 启动首行强制注入 `FLAGS_enable_pir_in_executor=0` 等 4 个核心环境变量。
    *   **构造器压制**：在代码层显式传入 `use_mkldnn=False`，实现双重保险，彻底杜绝 `Unimplemented` 报错。

### 2. GUI 交互体验全方位完善
*   **隐私保护**：API Key 默认星号掩码显示，新增“显示/隐藏”眼睛按钮，方便核对。
*   **智能填充**：实现“零配置启动”。若配置文件缺失，自动从脚本源码中正则表达式提取硬编码的默认 API Key 和目录。
*   **模式切换**：新增“截图模式”选择（智能识别 vs 固定频率）。智能模式下，频率输入框自动置灰禁用，UI 逻辑清晰。
*   **一键停止**：在面板增加红色“🛑 停止处理”按钮。通过全局 `STOP_REQUESTED` 信号和 `InterruptedError` 异常抛出，实现长耗时任务的毫秒级安全中止。

### 3. 性能与健壮性优化
*   **内存回收**：在 OCR 循环中引入 `gc.collect()`，每处理一帧清理一次内存，防止处理数百张截图时内存溢出。
*   **实时进度**：在 GUI 日志框中实时滚动显示 OCR 扫描进度（如 `[40/285]`），拒绝界面“假死”。

---

## 📅 2026-04-23 (核心重构与稳定性升级)
*(...内容同前...)*

---

## 🛠 待办事项
- [x] **智能 OCR 截图**：已实现语义级翻页识别、终极报错修复与模式切换。
- [x] **一键停止机制**：GUI 按钮与后台信号检查点已全链路打通。
- [ ] **PDF 直出排版**：支持直接导出排版精美的 PDF 格式教材。
- [ ] **硬件加速自适应**：自动检测 CUDA 环境并切换 GPU 模式。

---
最后更新: 2026-04-24 (环境彻底稳定，GUI 完成度大幅提升)
2026年4月24日
